// Exercise 01: default tasks, task dependencies

defaultTasks 'clean', 'compile'

task clean << {
	println 'Executing clean task'
}

task compile << {
	println 'Executing compile task'
}

task other(dependsOn: 'compile') << {
	println 'Not a default task'
}
